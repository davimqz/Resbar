{
  "buildCommand": "npm install -g pnpm@9 && pnpm install && pnpm --filter @resbar/shared build && pnpm --filter web build",
  "outputDirectory": "apps/web/dist",
  "installCommand": "echo skip",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
