{
  "buildCommand": "cd ../.. && corepack enable && corepack prepare pnpm@latest --activate && pnpm install && cd packages/shared && pnpm build && cd ../../apps/web && pnpm build",
  "installCommand": "corepack enable && corepack prepare pnpm@latest --activate && pnpm install",
  "framework": "vite",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
