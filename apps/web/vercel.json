{
  "installCommand": "echo 'Skipping default install...'",
  "buildCommand": "npm install -g pnpm && cd ../.. && pnpm install && cd packages/shared && pnpm build && cd ../../apps/web && pnpm build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
